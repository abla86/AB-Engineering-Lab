# SYSTEMS LAB — LEGACY

Historical browser-based systems laboratory.

## Canonical destination

Reusable systems demonstrations have been consolidated into `abla86/AB-Engineering-Lab/labs/engines/`.

Migrated implementations:

- `labs/engines/wasm-benchmark.js` — native WebAssembly benchmark
- `labs/engines/worker-pool.js` — native Web Worker pool

The source repository is retained for provenance. New development belongs in `AB-Engineering-Lab`.
