# Kotlin showcase

Typed event processor kept deliberately small: **data classes**, `require` validation, `EventType.entries` and **kotlin.test** tests on the JVM.

    gradle test

Requires JDK 21 and Gradle 8+. CI: `.github/workflows/technology-lab.yml`.
