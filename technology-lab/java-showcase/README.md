# Java showcase

Small event-processing component demonstrating **Java 21 records** (validated in the compact constructor), **streams** and **JUnit 5**.

    mvn -B test

Requires JDK 21 and Maven. CI: `.github/workflows/technology-lab.yml`.
